def apply_style(content_img, style_img, model):
    """
    Placeholder cho hàm apply_style.

    Args:
        content_img (np.ndarray): ảnh nội dung (H, W, C)
        style_img (np.ndarray): ảnh style (H, W, C)
        model (str): "adain", "sanet"

    Returns:
        np.ndarray: ảnh kết quả (H, W, C)
    """
    # Khang Hy code hàm này
    # TODO: replace logic này bằng style transfer thực tế
    pass
